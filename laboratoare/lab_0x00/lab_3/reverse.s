# given a string stored in memory 's' and a memory space 't'
# compute reverse of string 's' into 't'
.data



.text
.global main
main:













    movl $1, %eax
    xorl %ebx, %ebx
    int $0x80
